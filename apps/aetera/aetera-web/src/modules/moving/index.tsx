import { Truck } from "lucide-react";
import { guideModule } from "../guide/module";
import { MOVING_MODULE_ID } from "./id";

export const movingModule = guideModule({
  id: MOVING_MODULE_ID,
  title: "이사 준비",
  icon: Truck,
});
