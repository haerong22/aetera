import type { ComponentType } from "react";

/**
 * 할 일 키 → 그 자리에 붙일 도구.
 *
 * 서버에 "이 항목에 계산기가 있다"를 따로 물어보지 않는다. 할 일 키는 이미 응답에 있고,
 * 콘텐츠가 **배포 후 바꾸지 않기로 한 안정적인 계약**이다(사용자의 체크 상태가 이 키로 저장된다).
 * 도구 화면은 어차피 프론트에 있으니, 어디에 붙일지도 프론트가 정하면 된다.
 *
 * 엔진은 이 표를 받아 키로 찾아 붙이기만 한다. 도구의 내용을 엔진이 알면
 * 퇴사 가이드의 계산기를 이사 가이드도 함께 짊어지게 된다.
 *
 * [api.ts] 가 아니라 여기 사는 이유: 저건 서버가 보내는 것들의 모양이고,
 * 이건 서버가 모르는 화면 배선이다.
 */
export type TaskToolMap = Record<string, ComponentType>;
