import { Briefcase } from "lucide-react";
import { guideModule } from "../guide/module";
import { JOB_CHANGE_MODULE_ID } from "./id";

export const jobChangeModule = guideModule({
  id: JOB_CHANGE_MODULE_ID,
  title: "이직 준비",
  icon: Briefcase,
});
